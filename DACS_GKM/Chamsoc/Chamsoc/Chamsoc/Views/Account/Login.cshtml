@model Chamsoc.ViewModels.LoginViewModel
@{
    ViewData["Title"] = "Đăng nhập";
}

<div class="login-container auth-container">
    <div class="login-background">
        <div class="medical-pattern"></div>
    </div>
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-6 col-lg-5">
                <div class="medical-card auth-card">
                    <div class="card-header-medical auth-card-header">
                        <div class="medical-icon login-icon">
                            <i class="fas fa-stethoscope"></i>
                        </div>
                        <h2 class="fw-bold">@ViewData["Title"]</h2>
                        <p class="text-muted">Đăng nhập để tiếp tục sử dụng dịch vụ chăm sóc</p>
                    </div>
                    <div class="card-body p-4">
                        <form asp-controller="Account" asp-action="Login" asp-route-returnurl="@ViewData["ReturnUrl"]" method="post">
                            
                            <div class="form-floating mb-3 medical-input-group">
                                <input asp-for="Username" class="form-control medical-input" placeholder="Tên đăng nhập" />
                                <label asp-for="Username"><i class="fas fa-user me-2"></i>Tên đăng nhập</label>
                                <span asp-validation-for="Username" class="text-danger"></span>
                            </div>
                            
                            <div class="form-floating mb-3 medical-input-group">
                                <input asp-for="Password" class="form-control medical-input" id="password" type="password" placeholder="Mật khẩu" />
                                <label asp-for="Password"><i class="fas fa-lock me-2"></i>Mật khẩu</label>
                                <button type="button" class="password-toggle" onclick="togglePassword()">
                                    <i class="fas fa-eye" id="passwordToggleIcon"></i>
                                </button>
                                <span asp-validation-for="Password" class="text-danger"></span>
                            </div>

                            <div class="form-check mb-3 medical-checkbox">
                                <input asp-for="RememberMe" class="form-check-input" id="RememberMe" />
                                <label for="RememberMe" class="form-check-label">Ghi nhớ đăng nhập</label>
                            </div>

                            
                            <div class="d-grid gap-2">
                                <button type="submit" class="btn medical-btn-primary btn-lg">
                                    <i class="fas fa-sign-in-alt me-2"></i>Đăng nhập
                                </button>
                            </div>
                        </form>

                        <div class="text-center mt-4">
                            <p class="mb-0">Chưa có tài khoản? <a asp-action="Register" class="medical-link fw-bold">Đăng ký ngay</a></p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Styles {
    <link href="~/css/common/auth-common.css" rel="stylesheet" />
    <link href="~/css/pages/login.css" rel="stylesheet" />
}

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
    <script>
        function togglePassword() {
            const passwordInput = document.getElementById('password');
            const toggleIcon = document.getElementById('passwordToggleIcon');
            
            if (passwordInput.type === 'password') {
                passwordInput.type = 'text';
                toggleIcon.classList.remove('fa-eye');
                toggleIcon.classList.add('fa-eye-slash');
            } else {
                passwordInput.type = 'password';
                toggleIcon.classList.remove('fa-eye-slash');
                toggleIcon.classList.add('fa-eye');
            }
        }

        // Thêm hiệu ứng focus cho các input
        document.querySelectorAll('.medical-input').forEach(input => {
            input.addEventListener('focus', function() {
                this.parentElement.classList.add('focused');
            });
            
            input.addEventListener('blur', function() {
                if (!this.value) {
                    this.parentElement.classList.remove('focused');
                }
            });
        });
    </script>
}